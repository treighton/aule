---
name: "contract-tester: Test-api"
description: "Generate and run API contract tests with agentic diagnosis"
category: Workflow
tags: [workflow, contract-tester, test-api]
---

Run API contract tests against a live endpoint.

**Usage:** `/test-api <spec-path> <base-url>`

This command invokes the contract-tester skill with the given OpenAPI spec path and API base URL. It generates tests, executes them, and reports results.
